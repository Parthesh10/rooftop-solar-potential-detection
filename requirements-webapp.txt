# Serving dependencies for webapp/ — deliberately NOT the training stack.
#
#   pip install -r requirements-webapp.txt
#   python -m webapp
#
# Notably absent: torch and segmentation-models-pytorch (~2.5 GB installed).
# The app runs the exported ONNX graph, so onnxruntime (~50 MB) is all it needs.
# webapp/inference.py will fall back to torch if no .onnx is present, but that
# path exists for development convenience, not for deployment.

# Pinned to what .venv actually has installed and this project actually runs
# against, not to whatever was newest when this line was written. Verified
# 2026-09-10: pyproj==3.8.1 (the previous pin) does not exist on PyPI at all —
# this file had never been installed from cleanly, only edited by hand, and
# `pip install -r requirements-webapp.txt` would have failed on line one of
# webapp/README.md's own deploy instructions.
fastapi==0.141.1
uvicorn[standard]==0.52.4
onnxruntime==1.29.0     # CPU. Swap for onnxruntime-gpu if a CUDA card is present.
pyproj==3.7.2           # geodesic area on WGS84 — never measure area in Mercator
httpx==0.28.1           # async tile fetching + PVGIS
numpy==2.4.4
pillow==12.2.0
opencv-python==5.0.0.93 # mask -> contours -> simplified polygons; swap for
                        # opencv-python-headless in a container (no libGL there)

# Only needed to (re-)export the model — not to serve it:
#   torch, segmentation-models-pytorch, onnx
