# Rooftop Solar Potential Detection — pinned dependencies
#
# Install torch FIRST from the CUDA-matched index, then the rest:
#
#   python -m venv .venv
#   .venv\Scripts\activate
#   pip install torch==2.8.0 torchvision==0.23.0 --index-url https://download.pytorch.org/whl/cu126
#   pip install -r requirements.txt
#
# torch is NOT listed below on purpose — the PyPI Windows wheel is CPU-only, so
# it must come from the pytorch index above or you get no CUDA.
#
# Version notes (verified on this machine, 2026-08-11):
#   * Python 3.13 is what is installed here. torch 2.5.x has no cp313 wheels;
#     2.6.0+ does. cu126 carries the widest range (2.6.0 -> 2.13.0).
#   * A cu126 build runs fine on a newer driver (this box reports CUDA 13.1) —
#     the CUDA runtime is forward-compatible with the driver.
#   * For CPU-only, swap the index URL for .../whl/cpu
# See F-22 in potential-fixes.md.

numpy
scipy
pillow
opencv-python
matplotlib
scikit-learn
tqdm

# Optional — richer augmentation (the dataloader falls back to torchvision
# transforms if this is absent, so it is not required to train).
# albumentations==1.4.21

# Optional — metric cross-checks in tests/
# torchmetrics==1.6.0

# Dev
pytest==8.3.4
